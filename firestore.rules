rules_version = '2';

// M0 placeholder — deny-by-default.
// Full rules will be implemented in M1.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
